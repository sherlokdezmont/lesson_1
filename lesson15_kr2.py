from lesson15_kr1 import setters

setters = ['lager',
       'ale',
       'porter',
       'lambic',
       'stout',
       'lager',
       'ale'
       ]
for i in range(len(setters)):
    print(i,setters[i])
    
print(setters[2])
print(len(setters))
